
:root {
    /* Updated Palette */
    --accent: #FF1694; /* Hot Pink */
    --accent-glow: rgba(255, 22, 148, 0.4);

    /* Vibrant Dark Backgrounds */
    --bg-primary: #050204; /* Almost black with a tiny hint of red */
    --bg-secondary: #14050b; /* Deep dark plum/black */

    --text-primary: #fdf2f8; /* Off-white with pink tint */
    --text-secondary: #cbd5e1;

    --card-hover: #1f0a14;
    --font-main: 'Inter', sans-serif;
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: var(--font-main);
    background-color: var(--bg-primary);
    color: var(--text-primary);
    line-height: 1.6;
    overflow-x: hidden;
}

a {
    text-decoration: none;
    color: inherit;
    transition: color 0.3s ease;
}

a:hover {
    color: var(--accent);
}

/* Utilities */
.container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
}

.section-title {
    font-size: 2.5rem;
    margin-bottom: 2rem;
    text-align: center;
    position: relative;
    padding-bottom: 10px;
}

.section-title::after {
    content: '';
    position: absolute;
    width: 60px;
    height: 4px;
    background: var(--accent);
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    border-radius: 2px;
    box-shadow: 0 0 15px var(--accent);
}

/* Header / Hero */
header {
    /* Dark vibrant overlay using the pink color */
    background: linear-gradient(135deg, rgba(5, 2, 4, 0.95) 0%, rgba(20, 5, 11, 0.9) 100%),
                url('https://images.unsplash.com/photo-1550751827-4bd374c3f58b?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80');
    background-size: cover;
    background-position: center;
    border-bottom: 1px solid rgba(255, 22, 148, 0.1);
    padding: 2rem 0;
    position: relative;
}

.hero {
    min-height: 85vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding-top: 60px;
}

.hero h1 {
    font-size: 3.5rem;
    margin-bottom: 0.5rem;
    /* Gradient text effect using the pink */
    background: linear-gradient(to right, #ffffff, #FF1694);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: fadeInDown 1s ease;
    text-transform: uppercase;
    letter-spacing: 2px;
}

.hero h2 {
    font-size: 1.2rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    font-weight: 300;
    animation: fadeInUp 1s ease 0.3s forwards;
    opacity: 0;
    border: 1px solid var(--accent);
    padding: 5px 15px;
    border-radius: 20px;
    background: rgba(255, 22, 148, 0.05);
}

.profile-img-container {
    margin-bottom: 1.5rem;
    animation: fadeInDown 1s ease;
}

.profile-img {
    width: 180px;
    height: 180px;
    border-radius: 50%;
    object-fit: cover;
    /* The pink border */
    border: 3px solid var(--accent);
    box-shadow: 0 0 30px var(--accent-glow);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.profile-img:hover {
    transform: scale(1.05);
    box-shadow: 0 0 50px var(--accent-glow);
}

.contact-info {
    display: flex;
    gap: 15px;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 1.5rem;
    animation: fadeInUp 1s ease 0.6s forwards;
    opacity: 0;
}

.contact-item {
    background: rgba(255, 255, 255, 0.05);
    padding: 10px 20px;
    border-radius: 50px;
    border: 1px solid rgba(255, 22, 148, 0.2);
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.9rem;
}

.contact-item:hover {
    background: var(--accent);
    color: #fff;
    border-color: var(--accent);
    box-shadow: 0 0 20px var(--accent-glow);
    transform: translateY(-3px);
}

/* Navigation */
nav {
    position: fixed;
    top: 0;
    width: 100%;
    background: rgba(5, 2, 4, 0.95);
    backdrop-filter: blur(10px);
    z-index: 1000;
    padding: 1rem 0;
    border-bottom: 1px solid rgba(255, 22, 148, 0.1);
}

.nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-weight: 700;
    font-size: 1.5rem;
    color: var(--accent);
    letter-spacing: 1px;
}

.nav-links {
    display: flex;
    gap: 2rem;
    list-style: none;
}

.nav-links a {
    font-weight: 500;
    font-size: 0.95rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.nav-links a:hover {
    color: var(--accent);
    text-shadow: 0 0 10px var(--accent-glow);
}

/* Sections */
section {
    padding: 5rem 0;
}

section:nth-child(even) {
    background: var(--bg-secondary);
}

/* Skills Grid */
.skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 2rem;
}

.skill-category {
    background: var(--bg-primary);
    padding: 2rem;
    border-radius: 16px;
    box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.5);
    border: 1px solid rgba(255, 22, 148, 0.1);
    transition: transform 0.3s ease, border-color 0.3s ease;
}

.skill-category:hover {
    transform: translateY(-5px);
    border-color: var(--accent);
}

.skill-category h3 {
    color: var(--accent);
    margin-bottom: 1.5rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding-bottom: 0.8rem;
    display: flex;
    align-items: center;
    gap: 10px;
}

.skill-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
}

.tag {
    background: rgba(255, 22, 148, 0.1);
    color: #fff;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    font-size: 0.85rem;
    border: 1px solid rgba(255, 22, 148, 0.2);
    transition: background 0.2s ease;
}

.tag:hover {
    background: var(--accent);
    border-color: var(--accent);
}

/* Timeline / Experience */
.timeline-item {
    background: var(--bg-primary);
    padding: 2rem;
    margin-bottom: 1.5rem;
    border-radius: 12px;
    border-left: 4px solid var(--accent);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    transition: transform 0.3s ease;
    border-right: 1px solid transparent;
    border-top: 1px solid transparent;
    border-bottom: 1px solid transparent;
}

.timeline-item:hover {
    transform: translateX(5px);
    border-color: rgba(255, 22, 148, 0.2);
    box-shadow: 0 4px 25px rgba(255, 22, 148, 0.1);
}

.timeline-header {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    margin-bottom: 1rem;
    align-items: baseline;
}

.role-title {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--text-primary);
}

.company-name {
    color: var(--accent);
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.9rem;
    letter-spacing: 1px;
}

.date-range {
    color: var(--text-secondary);
    font-size: 0.9rem;
    font-style: italic;
    background: rgba(255, 255, 255, 0.05);
    padding: 2px 8px;
    border-radius: 4px;
}

.description-list {
    list-style-position: inside;
    color: var(--text-secondary);
    margin-top: 1rem;
}

.description-list li {
    margin-bottom: 0.8rem;
}

.cert-box {
    background: linear-gradient(45deg, var(--bg-primary), var(--bg-secondary));
    padding: 2rem;
    border-radius: 12px;
    border: 1px solid var(--accent);
    text-align: left;
}

/* Footer */
footer {
    background: #000;
    padding: 3rem 0;
    text-align: center;
    border-top: 1px solid rgba(255, 22, 148, 0.2);
    color: var(--text-secondary);
}

footer a:hover {
    color: var(--accent);
}

/* Animations */
@keyframes fadeInDown {
    from { opacity: 0; transform: translateY(-30px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

/* Responsive */
@media (max-width: 768px) {
    .nav-links {
        display: none;
    }

    .hero h1 {
        font-size: 2.5rem;
    }

    .timeline-header {
        flex-direction: column;
        gap: 5px;
    }
}